Elèves : Soumaya MEDIOUNI & Lucas AUCLAIR en LDDBI

Fichier .py:
Tout le code ainsi que les commentaires sont écrits en anglais pour simplifier la réutilisation du code et la compréhension de celui-ci et ce même en dehors de ce projet.
Pour répondre aux questions, il suffit d'aller dans la section 'Main', et d'enlever les # devant les print de fonction. Toute indication relative à l'usage ou pour clarifier le fonctionnement du fichier et des fonctions est écrite en anglais en commentaire dans la même section afin de simplifier la compréhension du code et la lecture de celui-ci.

Tout marche sauf la fonction 'attack' de la question 6, qui pour le moment semble ne jamais trouver la configuration initiale ou la clé secrète du générateur. Le problème étant soit un problème quelconque au sein de la fonction, soit un problème relatif à la méthode de l'attaque (peu probable) ou alors une omission lors de la mise en place du processus (en tout cas avec les octets aléatoires du CSS utilisés pour tester la fonction). La fonction est donc plus dans un état de débuggage qu'elle n'est réellement utilisable.

Le code est segmenté dans l'ordre:
	- imports
	- fonctions
	- tests et validations/vérifications des différentes fonctions en commentaires
	- main => pour répondre aux questions posées dans le sujet du projet, avec donc tout en commentaire pour vérifier chaque question une par une au choix.

Les questions 4 & 5 sont dans le fichier PDF.